---
interface Props {
  slug: string;
  title: string;
  excerpt: string;
  date: Date;
  coverImage?: string;
}

const { slug, title, excerpt, date, coverImage } = Astro.props;
const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<a
  href={`/blog/${slug}/`}
  class="group block overflow-hidden rounded-xl border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg-elevated))] transition-all duration-[var(--transition-base)] hover:border-[rgb(var(--color-accent))]/40 hover:shadow-glow"
>
  {coverImage && (
    <div class="aspect-video overflow-hidden bg-[rgb(var(--color-border))]/30">
      <img
        src={coverImage}
        alt={title}
        class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
      />
    </div>
  )}
  <div class="p-6">
    <p class="text-sm text-[rgb(var(--color-text-muted))]">{formattedDate}</p>
    <h3 class="mt-2 font-display text-display-sm font-semibold text-[rgb(var(--color-text))] group-hover:text-[rgb(var(--color-accent))]">
      {title}
    </h3>
    <p class="mt-2 line-clamp-2 text-sm text-[rgb(var(--color-text-muted))]">{excerpt}</p>
    <p class="mt-3 text-sm font-medium text-[rgb(var(--color-accent))]">Read more →</p>
  </div>
</a>
